= form_with(model: cart_item, url: cart_cart_item_path(cart_item), method: :patch) do |f|
  .hstack.gap-3
    = f.number_field :quantity, class: "form-control"
    = f.submit '更新する', class: 'btn btn-outline-danger btn-sm'
