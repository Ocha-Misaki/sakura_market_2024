- size ||= "295x145"
- type ||= :thumbnail
- class_name ||= ""
.book-img
  - image = resource.image.attached? ? resource.image.variant(type) : "no-img.jpg"
  = image_tag(image, class: "object-fit-cover #{class_name}", size: size)
